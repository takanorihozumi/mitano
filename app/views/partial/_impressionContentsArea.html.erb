<div class="col-md-9 col-sm-9 col-xs-12 col-md-offset-2 portfolio-item row-space-2 impression-overview">
  <div class="portfolio-caption">
    <span style="font-weight: bold;"></span>
    <div class="balloon5">
      <div class="faceicon">
        <% if !impression.user.image.nil? %>
        <%= image_tag impression.user.image, class:"img-circle profile-0" %>
        <% else %>
        <%= image_tag 'no-image', class:"img-circle profile-0" %>
        <% end %>
        <p><%= impression.user.name %>さん</p>
      </div>

      <div class="chatting">
        <div class="says">
          <p><%= simple_format(impression.impression) %></p>
          <div id="impression-<%= impression.id %>" data-impression-id="<%= impression.id %>">
            <div class="btn-toolbar">
              <div class="btn-group">
                <%= render "likes/like",impression: impression %>
              </div>
              <div class="btn-group">
                <%= link_to impression_path(impression), class: 'btn btn-default btn-sm' do %>
                <span class="glyphicon glyphicon-edit"></span> コメントする
                <% end %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>